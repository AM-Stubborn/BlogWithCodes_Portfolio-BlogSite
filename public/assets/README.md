# Media assets

Place trek/travel photos in `places/` and blog cover images in `blog/`.

Reference them from content JSON with paths like:

- `assets/places/triund.jpg`
- `assets/blog/ai-engineering-basic/01.png`

Do not use remote image URLs in content JSON — keep media in this folder so GitHub Pages stays self-contained.
